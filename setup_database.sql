-- Create the polling database
CREATE DATABASE IF NOT EXISTS polling_db;

-- Use the database
USE polling_db;

-- Show that database is created
SELECT 'Database polling_db created successfully!' as message;
